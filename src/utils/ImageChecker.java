package utils;

import org.sikuli.script.Pattern;

public class ImageChecker {
    static String imagePath = System.getProperty("user.dir") + "\\";
    public static final Pattern IMAGE = new Pattern(imagePath + "images/test");

    // defined your provided image path below
}